# Railway ignora estos paths al construir el servicio backend.
# Mantiene el deploy chico y evita que Railpack confunda el stack
# detectando node_modules / package.json del frontend.

# Frontend (se deploya aparte en Vercel/Netlify/etc.)
frontend/

# Documentación y SDD artifacts (no afectan runtime)
docs/
openspec/
testsprite_tests/
.engram/

# Editor / OS / tooling local
.vscode/
.idea/
.DS_Store
.claude/
.agents/
.agent/
.atl/
.opencode/
.pi/

# Caches de tests/lint
.pytest_cache/
.ruff_cache/
backend/__pycache__/
**/__pycache__/

# Local env y artefactos
.env.local
*.log

# Docker (Railway lo ignora igual, pero por claridad)
docker-compose.yml
backend/Dockerfile.dev
frontend/Dockerfile.dev
.dockerignore
