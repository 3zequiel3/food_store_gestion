# Alembic configuration for Food Store backend.
# Run migrations from the backend/ directory: cd backend && python -m alembic <cmd>

[alembic]
# script_location is relative to this ini file (backend/alembic/)
script_location = alembic

# Sortable filename template
file_template = %%(year)d%%(month).2d%%(day).2d_%%(hour).2d%%(minute).2d_%%(rev)s_%%(slug)s

# DATABASE_URL is read from env in env.py — do NOT set it here
# sqlalchemy.url = ...

prepend_sys_path = ..

path_separator = os

# output_encoding = utf-8

[post_write_hooks]
# (no post-write hooks configured)

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
